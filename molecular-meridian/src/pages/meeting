---
const SITE_TITLE = "RAPDM | Meeting";
const MEETING_URL = "REPLACE_WITH_GOOGLE_APPOINTMENT_SCHEDULE_URL";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{SITE_TITLE}</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <style>
      :root{
        --bg:#f6f7f9; --ink:#0b1220; --line: rgba(11,18,32,.10);
        --surface: rgba(255,255,255,.78); --shadow: 0 10px 26px rgba(15,23,42,.10);
        --r: 18px;
        --max: 980px;
      }
      body{ margin:0; font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial; background:var(--bg); color:var(--ink); }
      .container{ width:min(var(--max), calc(100% - 28px)); margin: 18px auto 28px; }
      .top{ display:flex; align-items:center; justify-content:space-between; gap:12px; margin-bottom: 14px; }
      a{ text-decoration:none; color: rgba(11,18,32,.86); border-bottom: 1px solid rgba(11,18,32,.20); padding-bottom: 2px; }
      .card{ background: var(--surface); border:1px solid var(--line); border-radius: var(--r); box-shadow: var(--shadow); overflow:hidden; }
      .frame{ width:100%; height: 78vh; border:0; display:block; }
      .hint{ font-size: 13px; color: rgba(11,18,32,.62); line-height:1.55; padding: 14px; }
      .btn{ display:inline-flex; align-items:center; gap:10px; border-radius:999px; padding: 12px 14px; font-size: 13px;
        border:1px solid rgba(11,18,32,.10); background: rgba(255,255,255,.74); box-shadow: var(--shadow); }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="top">
        <a href="/">Back</a>
        <a class="btn" href={MEETING_URL} target="_blank" rel="noreferrer">Open in Google Calendar</a>
      </div>

      <div class="card">
        {MEETING_URL.includes("REPLACE_WITH") ? (
          <div class="hint">
            Paste your Google Calendar appointment schedule URL in <strong>src/pages/meeting.astro</strong> and in <strong>src/pages/index.astro</strong>.
          </div>
        ) : (
          <iframe class="frame" src={MEETING_URL} loading="lazy" referrerpolicy="no-referrer"></iframe>
        )}
      </div>
    </div>
  </body>
</html>
